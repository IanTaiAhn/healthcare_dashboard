# Backend API URL — leave empty for local development (Vite proxy handles it).
# Set this when building for production deployment.
# Example: VITE_API_BASE_URL=https://utah-healthcare-workforce.onrender.com
VITE_API_BASE_URL=
