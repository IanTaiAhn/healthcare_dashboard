# TURNED OFF FOR NOW
# name: Deploy

# on:
#   push:
#     branches: [main]
#   workflow_dispatch:

# jobs:
#   # ── Backend: Deploy to Render ──────────────────────────────────────────────
#   # Render auto-deploys from GitHub on push to main via its dashboard config.
#   # This job runs linting and tests before Render picks up the deploy.
#   backend-checks:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: ./backend
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install uv
#         run: curl -LsSf https://astral.sh/uv/install.sh | sh

#       - name: Install dependencies
#         run: uv pip install --system -e ".[dev]"

#       - name: Lint
#         run: ruff check .

#       - name: Run tests
#         run: pytest tests/ -v --tb=short
#         # Tests will be added incrementally — this will pass with no test files

#   # ── Frontend: Build and deploy to GitHub Pages ─────────────────────────────
#   frontend-deploy:
#     runs-on: ubuntu-latest
#     needs: backend-checks
#     defaults:
#       run:
#         working-directory: ./frontend
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "npm"
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         run: npm ci

#       - name: Build
#         run: npm run build
#         env:
#           VITE_API_BASE_URL: ${{ vars.API_BASE_URL }}
#           # Set API_BASE_URL as a repository variable in GitHub Settings
#           # e.g., https://utah-healthcare-workforce.onrender.com

#       - name: Deploy to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./frontend/dist
